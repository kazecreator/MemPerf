---
name: Bridge example request
about: Request a bridge script for a specific memory system
---

**Memory system**
Name and link:

**Interface** (what methods does it expose?)
```python
# e.g.
mem.add(text, metadata={})
mem.search(query, limit=10)
```

**Install command**
```bash
pip install ...
```
